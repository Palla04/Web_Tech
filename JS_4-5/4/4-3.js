// 1. Create a Boolean value for an ID variable
let hasID = true; // Change to false to test the other case

// 2. Use a ternary operator to check ID validity
let message = hasID 
  ? "You are allowed to enter the venue." 
  : "You are not allowed to enter the venue.";

// 3. Output the response to the console
console.log(message);
